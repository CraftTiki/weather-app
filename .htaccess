# Cache Control for Weather App

# HTML - short cache, always revalidate
<FilesMatch "\.html$">
    Header set Cache-Control "no-cache, must-revalidate, max-age=300"
</FilesMatch>

# CSS and JS - cache for 1 year (we use version strings to bust cache)
<FilesMatch "\.(css|js)$">
    Header set Cache-Control "public, max-age=31536000"
</FilesMatch>

# Images - cache for 1 week
<FilesMatch "\.(ico|jpg|jpeg|png|gif|svg|webp)$">
    Header set Cache-Control "public, max-age=604800"
</FilesMatch>
